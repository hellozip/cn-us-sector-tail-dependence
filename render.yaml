services:
  - type: web
    name: cn-us-sector-tail-dependence
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 900
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.13

